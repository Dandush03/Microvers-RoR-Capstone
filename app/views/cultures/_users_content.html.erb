<section class="user-menu">
  <nav>
    <%= link_to 'tweets', url_for( :id => @user.id, :menu => nil, :partial => 'tweets') %>
    <%= link_to 'fallowings', url_for( :id => @user.id, :menu => 1, :partial => 'fallowings') %>
    <%= link_to 'fallowers', url_for( :id => @user.id, :menu => 1, :partial => 'fallowers') %>
  </nav>
</section>
<section class="user-menu-info">
  <div id="content">
    <%= render @partial, :users => get_object(@user,@partial_name) %>
  </div>
</section>
